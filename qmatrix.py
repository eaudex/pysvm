import sys
class qMatrix:
	def get_Q(self, col, len):
		print "Inherits from QMatrix but failed to define"
		sys.exit(1)
	def get_QD(self):
		print "Inherits from QMatrix but failed to define"
		sys.exit(1) 
	def swap_index(self, i, j):
		print "Inherits from QMatrix but failed to define"
		sys.exit(1) 
